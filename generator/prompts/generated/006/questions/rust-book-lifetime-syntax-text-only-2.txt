

1. What does the 'static lifetime denote?

A. That the affected reference can live for the entire duration of the program.

B. That the affected reference can live for the entire duration of a function.

C. That the affected reference can live for the entire duration of a method.

D. That the affected reference can live for the entire duration of a trait.


2. What does the first rule of the lifetime elision rules specify?

A. That each parameter gets its own lifetime.

B. That the lifetime of one input parameter gets assigned to the output lifetime.

C. That the lifetime of self is assigned to all output lifetime parameters.

D. That the lifetime of all string literals is 'static.


3. Which of the following is NOT true about the third rule of the lifetime elision rules?

A. It specifies that the lifetime of self is assigned to all output lifetime parameters.

B. It makes methods much nicer to read and write because fewer symbols are necessary.

C. It applies in method signatures.

D. It only applies when there are multiple input lifetime parameters.


4. In order for the longest_with_an_announcement function to work, what trait must ann implement?

A. Iterator

B. Display

C. Eq

D. PartialOrd


5. Why might an error message suggesting the 'static lifetime result from attempting to create a dangling reference?

A. Because the reference isn't actually living the entire lifetime of the program.

B. Because the reference isn't actually living the entire lifetime of the function.

C. Because the reference isn't actually living the entire lifetime of the method.

D. Because the reference isn't actually living the entire lifetime of the trait object.